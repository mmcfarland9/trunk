/**
 * @deprecated This file re-exports from ./state/ for backward compatibility.
 * Update imports to use './state' (the directory) or specific modules:
 * - './state/migrations' for schema migrations
 * - './state/view-state' for navigation state
 * - './state/resources' for soil/water/sun
 * - './state/node-state' for node data, sprouts, leaves
 */

export * from './state/index'
