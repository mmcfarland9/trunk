pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      root: web/
      glob: "*.{ts,tsx}"
      run: npx tsc --noEmit

    - name: lint
      root: web/
      glob: "src/**/*.{ts,tsx}"
      run: npx biome lint {staged_files}
